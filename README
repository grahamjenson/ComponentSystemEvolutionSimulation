Simulation:
CM = update probability, install probability, weighted packages, Update MOF, Install MOF, 
USER = initial system, (time,request,cudf file, MOF)*

GUSER(CM) = USER
Simulation(USER) = (system)*


